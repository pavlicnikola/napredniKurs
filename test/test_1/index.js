// 1. Za dati HTML, navesti sta ce sledeca linija koda izvrsiti
        // const element = document.querySelector('.par').parentElement

// 2. Za dati HTML, objasniti sta radi navedeni kod. Gde se smesta noviParagraf?
        // const div2 = document.getElementById('div2')
        // const noviParagraf = document.createElement('p')
        // div2.children[1].appendChild(noviParagraf)

// 3. Objasniti sta ce se desiti u sledecem delu koda.
        // const lista = document.createElement('ul')

        // for(let i = 0; i < 3; i++) {
        //     const li = document.createElement('li')
        //     li.className = 'stavka'
        //     li.innerText = i
        //     lista.appendChild(li)
        // }

        // console.log(lista)

// 4. Napisati kod koji pravi select i dodaje 3 opcije(jezici koje osoba govori)

// 5. Sta radi sledeci kod?
        // const clanovi = document.querySelectorAll('.clan')
        // clanovi.forEach(clan => {
        //     clan.innerHTML = '<button> X </button>'
        // })